<%= form_with(model: discount_type) do |form| %>
  <% if discount_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discount_type.errors.count, "error") %> prohibited this discount_type from being saved:</h2>

      <ul>
        <% discount_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
